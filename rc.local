#!/bin/sh -e
#
# rc.local

sudo service cron start &

echo "
--> Starting server"
su pi -c 'USERR/git/fuettr_prototype/Web_Application/Webserver/ng2 && npm start >> /tmp/server.log 2>&1 &'

_IP=$(hostname -I) || true
if [ "$_IP" ]; then
  printf "My IP address is %s\n" "$_IP"
fi


exit 0