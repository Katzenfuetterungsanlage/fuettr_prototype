# Testserver for Java Application

## Setup

Install the node modules needed to run the server:

```bash
npm install
```

## Starting the Server

To start the server simply type:

```bash
npm start
```

then open the browser on [localhost](http://localhost/api/callMeMaybe).